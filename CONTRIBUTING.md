# Contributing

欢迎提 issue 和 PR。这是 AI 味指纹扫描器，检测中文文本里的机器腔，纯标准库零依赖。

## Bug 报告
- 附一段"被误判"或"该判未判"的中文文本 + 期望/实际检测结果
- 标注 Python 版本

## PR 要求
- 代码 + 对应测试；跑通 `python -m pytest test_zh_writing.py -v`
- 新增检测规则需附正/反例（该命中的和不该命中的各给一条），防止误伤正常写作
- 保持纯标准库零依赖，不引第三方
- 检测规则改动需保证既有用例仍通过（避免回归）

## 提交规范
- 提交信息：`feat:` / `fix:` / `chore:` / `docs:` 前缀 + 简述
- 版本号递增：改 `__version__` / README 徽标 / CHANGELOG 保持一致
- 规则命名和注释用中文，面向中文写作者
