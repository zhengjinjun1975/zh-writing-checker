# zh-writing-checker v0.1.2 更新公告

## 版本更新

zh-writing-checker 发布 v0.1.2。本次更新做两件事：把工具从"报问题的检测器"升级为"把稿子改得像人写的优化器"，并重构产品的方法论语言体系为**三线**（硬规则 / 文风 / 人味）。项目仍处早期，按小版本递增演进，不虚标大版本。

## 一句话方法论

**先判人味，再查三线，改完复检。**

改写前先问三个递进的问题：

1. **这是人写的吗？** —— 人味门检（human_gate），真人文本停手，只清格式，不动声口
2. **哪里不像人写的？** —— 三线检测（scan），按硬规则 / 文风 / 人味三条线定位问题
3. **怎么改得像人？** —— 改写建议 + 复检（refine），确认问题减少

## 本次更新内容

### 优化器能力（检测器 → 优化器）

一、人味门检（human_gate）。改写前先判断输入是不是真人写的。真人文本的特征，比如自纠语气、方言口语、自嘲、只有本人知道的细节，命中任一条就停手，只清理格式，不动语言本身。这一步防止把金庸、王朔式声口改坏，保护作者不可逆的写作印记。

二、语体识别（register_of）。区分八种语体，包括学术、公文、口语、自媒体、品牌广告等。不同语体的 AI 腔标准完全不同，先识别再动手，避免把学术论文的"进行了深入分析"误改成微信聊天，也避免把公文的"依法予以处理"改成大白话。

三、改写复检三步（refine）。检测定位问题，生成可执行的改写建议，再对建议结果复检，输出前后对比，让改稿效果可度量、可追踪。

### 方法论语言体系重构（三线，能力不变）

四、语言体系：六维编号 D1-D6 → 三线叙述（硬规则 / 文风 / 人味）。**记住三线，比记住六个编号容易。** JSON 保留 `layer: D1-D6` 兼容，新增 `lines` 三线汇总字段（纯增量，不破坏既有 CI 解析）。

五、代码极简重构：删死代码、抽统一收集、`scan()` 拆分为 6 个维度扫描函数 + 汇总函数，圈复杂度 39 → 0。已通过 CodeAgent 双栈审查（静态 0 告警）。

核心原则不变：严谨不等于僵化，正确性必改、风格给建议，改写不虚构，只在原文已有细节上打磨，最终取舍在作者。

## 全部能力

三线六维检测覆盖严谨性与文风。硬规则线（字词、标点、语法、数字）是必改项，文风线（去 AI 味）与人味线（可读性）给建议。D5 是重头，内置二十七条禁用词、元语言、教科书开头、句式指纹、死板动词、破折号等检测。输出结构化 JSON 报告，每条问题带层级、类型、次数、严重级别、修改建议和上下文，可直接交给 CI 消费。

## 在智能体中使用

一、命令行。对本机文档跑一遍，人类可读报告或 JSON 报告。

二、CI 检查器。在持续集成里加一步，对提交的中文内容自动扫描，fail 问题直接让流水线失败，把写作规范落进工程流程。

三、智能体技能。把工具的能力封装进智能体工作流，让智能体在改稿前先人味门检、先识别语体，再决定怎么改，避免智能体把真人的稿子改得千篇一律。

词表集中在文件头部常量，可按团队规范增删，适合接入特定领域的写作约束。

## 维护与展望

本项目保持持续更新。维护人会围绕写作、文字、去 AI 味三个方向长期调优：扩充禁用词与句式指纹库，完善语体识别覆盖，打磨改写建议的准确度，让工具更懂中文、更懂人味。也欢迎社区反馈误判案例，帮助工具越改越准。

项目处于 v0.1.x 早期，功能会持续迭代，接口可能调整，欢迎试用与反馈。

## 项目地址

GitHub: https://github.com/zhengjinjun1975/zh-writing-checker
